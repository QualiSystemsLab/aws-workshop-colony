var assert = require('assert');

describe('Auth api test', function () {
    
    it('api signIn strategy works when valid email and pass', function () {
    });
    
    it('api signIn strategy returns error when email or pass not valid', function () {
    });
 
    it('api signIn redirects to root when login failed', function () {
    });

    it('get profile method retuns user data for logged in user', function () {
    });

});